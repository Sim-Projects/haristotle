import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers/session-provider";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Haristotle - Write, Publish, Share",
  description: "A modern blog platform with a Notion-like editor. Share your thoughts and stories with the world.",
  keywords: ["blog", "writing", "publishing", "medium", "notion", "editor"],
  authors: [{ name: "Haristotle Team" }],
  openGraph: {
    title: "Haristotle - Write, Publish, Share",
    description: "A modern blog platform with a Notion-like editor.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        <Providers>
          {children}
          <Toaster position="top-right" />
        </Providers>
      </body>
    </html>
  );
}
